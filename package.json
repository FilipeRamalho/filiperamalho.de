{
  "name": "filiperamalho.de",
  "version": "6.0.0",
  "license": "UNLICENSED",
  "workspaces": ["frontend", "backend"],
  "scripts": {
    "dev:payload": "cd backend && bun run dev",
    "dev:website": "cd frontend && bun run dev",
    "build:payload": "cd backend && bun run build",
    "build:website": "cd frontend && bun run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}