{
  "name": "filiperamalho.de",
  "version": "6.0.0",
  "license": "UNLICENSED",
  "workspaces": ["frontend", "backend"],
  "scripts": {
    "dev:payload": "bun run --filter 'filiperamalho.de_backend' dev",
    "dev:website": "bun run --filter 'filiperamalho.de_frontend' dev",
    "build:payload": "bun run --filter 'filiperamalho.de_backend' build",
    "build:website": "bun run --filter 'filiperamalho.de_frontend' build",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}